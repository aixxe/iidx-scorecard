/* Overlay image with fixed text displayed on top of the background. */
Vue.component('card-frame', {
    template: `<div id="card-frame">
        <img src="./components/frame/images/base.png">
    </div>`
});